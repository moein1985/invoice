# 📝 راهنمای تست نسخه Web

## ✅ تست‌های انجام شده (بر اساس لاگ)

### 1. ورود و احراز هویت
- ✅ Login با `admin/admin123` - موفق
- ✅ Logout - موفق
- ✅ Re-login - موفق

### 2. مدیریت کاربران
- ✅ بارگذاری لیست کاربران (1 کاربر)
- ✅ ایجاد کاربر جدید - موفق
- ✅ بارگذاری مجدد لیست (2 کاربر)

### 3. مدیریت مشتریان
- ✅ بارگذاری لیست مشتریان
- ✅ ایجاد مشتری جدید - موفق

### 4. مدیریت اسناد
- ✅ بارگذاری لیست اسناد (0 سند اولیه)
- ✅ ایجاد پیش‌فاکتور موقت (PRO-1001) - موفق
  - محصول: yealink ip phone w70
  - قیمت خرید: 10,200,000
  - قیمت فروش: 12,444,000
  - سود: 2,244,000
- ✅ مشاهده جزئیات سند - موفق
- ✅ تبدیل پیش‌فاکتور موقت به پیش‌فاکتور - موفق
- ✅ بارگذاری مجدد لیست (2 سند: 1 temp, 1 proforma)
- ✅ تغییر تب بین انواع اسناد

### 5. کارتابل تأیید
- ⚠️ **بارگذاری کارتابل - هیچ سندی پیدا نشد**
  - علت: سند ایجاد شده `approvalStatus = notRequired` داشت
  - برای نمایش در کارتابل باید `approvalStatus = pending` باشد

---

## 🔍 تست‌های باقی‌مانده

### 1. تست کامل Approval Workflow

#### مرحله 1: ایجاد پیش‌فاکتور موقت با درخواست تأیید
1. رفتن به "مدیریت اسناد"
2. ایجاد پیش‌فاکتور موقت جدید
3. **مهم:** پس از ذخیره، روی دکمه "درخواست تأیید" کلیک کنید
   - این کار `approvalStatus` را به `pending` تغییر می‌دهد

#### مرحله 2: بررسی کارتابل تأیید
1. رفتن به "کارتابل تأیید"
2. باید سند با status `pending` نمایش داده شود
3. تست دکمه‌های تأیید/رد

#### مرحله 3: تأیید یا رد سند
- **تأیید:**
  - کلیک روی "تأیید"
  - سند باید از کارتابل حذف شود
  - `approvalStatus` به `approved` تغییر کند

- **رد:**
  - کلیک روی "رد"
  - وارد کردن دلیل رد
  - سند باید از کارتابل حذف شود
  - `approvalStatus` به `rejected` تغییر کند

### 2. تست Pagination (نیازمند داده بیشتر)

```
برای تست pagination:
1. حداقل 15-20 مشتری ایجاد کنید
2. رفتن به صفحه بعدی/قبلی
3. تغییر تعداد نمایش در صفحه
4. همین کار را برای اسناد و کاربران تکرار کنید
```

### 3. تست جست‌وجو و فیلتر

#### مشتریان:
- جست‌وجو با نام مشتری
- جست‌وجو با شماره تلفن
- جست‌وجو با ایمیل
- فیلتر فعال/غیرفعال

#### کاربران:
- جست‌وجو با نام کاربری
- فیلتر بر اساس نقش (employee, supervisor, manager, admin)
- فیلتر فعال/غیرفعال

#### اسناد:
- فیلتر بر اساس نوع (tempProforma, proforma, invoice, returnInvoice)
- فیلتر بر اساس وضعیت (paid, unpaid, pending)
- جست‌وجو در شماره سند

### 4. تست Offline Mode

```powershell
# 1. خاموش کردن backend
Get-Process -Name node | Stop-Process -Force

# 2. در برنامه web:
- ایجاد مشتری جدید (باید در cache ذخیره شود)
- ایجاد سند جدید (باید در cache ذخیره شود)

# 3. روشن کردن backend
cd C:\Users\Moein\Documents\Codes\invoice\invoice-backend
node src\server.js

# 4. در برنامه web:
- رفرش صفحه یا reload داده‌ها
- داده‌های cache باید sync شوند
```

### 5. تست CRUD کامل

#### برای هر موجودیت (Customer, User, Document):
- ✅ Create - انجام شد
- ⏳ Read/View - بررسی جزئیات
- ⏳ Update - ویرایش
- ⏳ Delete - حذف

### 6. تست Validation

- ورودی‌های خالی
- فرمت‌های نامعتبر (ایمیل، شماره تلفن)
- اعداد منفی در قیمت‌ها
- تاریخ‌های نامعتبر

### 7. تست UI/UX

- Responsive design (تغییر اندازه پنجره مرورگر)
- Loading indicators
- Error messages
- Success notifications
- RTL layout (راست به چپ)

---

## 🐛 مشکلات شناسایی شده

### 1. کارتابل تأیید خالی
**علت:** سند با `approvalStatus = notRequired` ایجاد شد  
**راه‌حل:** استفاده از دکمه "درخواست تأیید" پس از ایجاد سند

### 2. TODO در ApprovalBloc
```dart
// TODO: Get current user ID from auth
const currentUserId = 'supervisor_id'; // Placeholder
```
**راه‌حل:** باید user ID واقعی از AuthBloc گرفته شود

---

## 📊 نتیجه کلی

### عملکرد موفق:
- ✅ Authentication & Authorization
- ✅ CRUD Operations (Create تست شده)
- ✅ Navigation & Routing
- ✅ State Management (BLoC)
- ✅ Local Storage (Hive)
- ✅ Document Conversion
- ✅ Logging & Debugging

### نیاز به تست بیشتر:
- ⏳ Approval Workflow (نیاز به درخواست تأیید)
- ⏳ Pagination (نیاز به داده بیشتر)
- ⏳ Search & Filter
- ⏳ Offline Sync
- ⏳ Update & Delete Operations
- ⏳ Error Handling
- ⏳ Performance با داده زیاد

---

## 🚀 مراحل تست پیشنهادی

### روز 1: تست پایه
1. ✅ Login/Logout
2. ✅ Create operations
3. ⏳ View/Edit/Delete operations

### روز 2: تست Workflow
1. ⏳ Complete approval workflow
2. ⏳ Document conversions
3. ⏳ Multi-step processes

### روز 3: تست Edge Cases
1. ⏳ Offline mode
2. ⏳ Error scenarios
3. ⏳ Validation
4. ⏳ Large datasets

### روز 4: تست Performance
1. ⏳ 100+ customers
2. ⏳ 50+ documents
3. ⏳ Pagination stress test
4. ⏳ Concurrent operations

---

## 💡 نکات مهم

1. **درخواست تأیید:** برای نمایش سند در کارتابل، حتماً دکمه "درخواست تأیید" را بزنید

2. **Backend Connection:** مطمئن شوید backend در حال اجراست:
   ```powershell
   # بررسی
   Invoke-RestMethod -Uri "http://localhost:3000/health"
   ```

3. **Clear Cache:** اگر مشکلی دیدید:
   ```powershell
   flutter clean
   flutter pub get
   ```

4. **Debug Tools:** از F12 در مرورگر برای:
   - مشاهده Console logs
   - بررسی Network requests
   - Debug state changes

5. **Hot Reload:** در ترمینال Flutter زدن `r` برای hot reload

---

## ✅ چک‌لیست تست نهایی

- [ ] همه CRUD operations
- [ ] Approval workflow کامل
- [ ] Pagination با 20+ رکورد
- [ ] Search & Filter
- [ ] Offline sync
- [ ] Error handling
- [ ] Validation messages
- [ ] UI responsive
- [ ] RTL layout
- [ ] Performance acceptable

---

**آخرین بروزرسانی:** 2025-11-18  
**وضعیت:** در حال تست - فاز اولیه موفق
