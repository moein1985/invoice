# نکته مهم درباره dart:js و dart:js_interop

## وضعیت کنونی

گوگل اعلام کرده که `package:js` (نه `dart:js`) منسوخ شده است و باید به `dart:js_interop` مهاجرت کرد.

## تفاوت‌ها:

### 1. `package:js` (منسوخ شده) ❌
```yaml
dependencies:
  js: ^0.6.7  # این package منسوخ شده
```

### 2. `dart:js` (هنوز فعال) ✅
```dart
import 'dart:js' as js;  // این هنوز در Flutter SDK موجود و کار می‌کند
```
- این یک کتابخانه داخلی Dart است
- هنوز پشتیبانی می‌شود
- برای کارهای ساده کافی است
- **این همان چیزی است که ما استفاده می‌کنیم**

### 3. `dart:js_interop` (جدید، توصیه شده برای پروژه‌های جدید) 🆕
```dart
import 'dart:js_interop';
```
- API جدیدتر و type-safe تر
- نیاز به Flutter SDK 3.16+ دارد
- پیچیده‌تر برای پروژه‌های موجود

## تصمیم برای این پروژه

✅ **استفاده از `dart:js` (فعلی)** بهترین انتخاب است چون:

1. **کار می‌کند**: هنوز در Flutter SDK موجود و فعال است
2. **ساده است**: نیازی به refactor کردن کد نیست  
3. **پایدار است**: تا چند سال آینده پشتیبانی خواهد شد
4. **بدون dependency خارجی**: نیازی به package js نیست

## کد فعلی ما

```dart
import 'dart:js' as js;  // ✅ این درست است
import 'package:flutter/foundation.dart';
import 'package:invoice/core/models/sip_config.dart';

class WebSipService {
  void initialize(SipConfig config) {
    final jsCode = """...""";
    js.context.callMethod('eval', [jsCode]);  // ✅ کار می‌کند
  }
}
```

## مهاجرت به dart:js_interop (اختیاری - در آینده)

اگر در آینده خواستید به API جدید مهاجرت کنید:

```dart
import 'dart:js_interop';
import 'package:web/web.dart';

// نیاز به تعریف interface های JSExport دارد
@JS()
@staticInterop
class JsSIPUA {}

extension JsSIPUAExtension on JsSIPUA {
  external void start();
  external void stop();
}
```

ولی فعلاً نیازی به این پیچیدگی نیست!

## خلاصه

- ❌ `js: ^0.6.7` package را حذف کردیم (منسوخ شده)
- ✅ `dart:js` را نگه داشتیم (built-in و فعال)
- ⏳ `dart:js_interop` برای آینده (اختیاری)

**هیچ تغییری در کد نیاز نیست - همه چیز آماده تست است!** 🎉
